# Normalize line endings to LF in the repo and working tree (Prettier expects LF).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.jpg binary
*.jpeg binary
*.webp binary
