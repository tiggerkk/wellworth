/**
 * Shared form-field class strings. `.field-control` (in `src/index.css`) is the single source of
 * truth for a single-line field's chrome + height; `FIELD_CLASS` is the ubiquitous full-width
 * composition of it, replacing the per-screen `const inputClass = 'field-control w-full'`.
 */
export const FIELD_CLASS = 'field-control w-full'
