/**
 * Net Worth Monthly Entry — placeholder (Phase 2 M2). The real entry screen
 * (month selector, copy-forward, inline add/edit/delete, live HKD total, RESET/SAVE)
 * is built in M3.
 */
export function NetWorthEntry() {
  return (
    <div className="pb-4">
      <header className="sticky top-0 z-10 bg-bg/90 px-4 py-3 backdrop-blur">
        <h1 className="text-lg font-medium text-text-primary">Monthly Entry</h1>
      </header>
      <div className="px-4 py-12 text-center">
        <p className="text-sm text-text-secondary">Monthly entry is coming soon.</p>
      </div>
    </div>
  )
}
